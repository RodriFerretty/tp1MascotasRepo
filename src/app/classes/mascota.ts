export class Mascota {
     name: string
     photoUrl: string
     type: string

     constructor(name: string, photoUrl, type){
        this.name = name
        this.photoUrl = photoUrl
        this.type = type
     }
}
